# 0.1.2

## 更新摘要

chore: 完善 Skills 分发一致性

- 分发快照与验证运行产物隔离,保持交付内容完整、可复核.
- 文件清单采用跨平台一致的排序,便于校验已安装内容.

## diagram-codebase (架构速览与详解)

修改时间: 2026-09-09

1. 分发验证使用独立副本,不将源码扫描测试生成的缓存混入交付目录;统一文件清单排序,支持跨平台内容对账.

# 0.1.1

## 更新摘要

fix: 修复发布环境依赖隔离与提交摘要生成

- 验证和发布作业使用独立虚拟环境,使隔离解析进程可读取固定依赖.
- 提交信息只提取所选记录的最新更新摘要,拒绝缺失摘要和多份记录拼接.
- 对齐提交规范与发布输入检查,保留原有验证及单线发布流程.

## diagram-codebase (架构速览与详解)

修改时间: 2026-09-08

1. 修复自动发布中 C 源码解析依赖安装位置与隔离子进程不一致的问题,验证和发布分别建立虚拟环境并检查隔离导入,保留源码扫描测试与解析隔离.

# 0.1.0

## bootstrap-project-context (建立项目上下文基线)

修改时间: 2026-09-08

1. 新增软件项目持久上下文基线流程,用固定职责区分根级工作契约、计划、当前能力和已验证经验。
2. 增加完整、可补齐和冲突三态判断,并按空白项目或现有代码库证据选择写入分支。
3. 增加显式项目校准,按授权判断方向偏离并更新指定文档,区分稳定规则与活动纠偏,不创建天然最高优先级的纠错文件.
4. 将 PLAN 与 FEATURES 定义为项目级当前状态窗口,按影响独立决定是否更新,合并重复内容并保留有效证据与未解决风险,不为临时局部修改追加日志.

## capture-personal-knowledge (沉淀个人知识)

修改时间: 2026-09-08

1. 明确仅维护项目本地经验文档时不触发个人知识沉淀流程。
2. 将写入预检收紧到实际目标父目录,同时校验规范化路径、排除范围和操作系统大小写语义。

## diagram-codebase (架构速览与详解)

修改时间: 2026-09-08

1. 新增面向 HDL、嵌入式 C、Python、Skill 路由和笔记关系的离线架构分析流程,交付可编辑 draw.io 速览与独立 Markdown 详解。
2. 建立 inventory、scan、query/context 的有界取证链路,以内容哈希判断索引新鲜度,不依赖目标工程 Git 状态。
3. 将解析依赖分为纳入分发的 `tools/bundled/` 与本机忽略的 `tools/local/`,并补充结构检查、证据引用和视觉验收边界。

## distill-person-thinking (人物思维蒸馏与调用)

修改时间: 2026-09-08

1. 新增从公开可追溯资料蒸馏人物思维模型、更新模型及按明确请求应用模型的完整流程。
2. 将人物资料外置到受检的 `profiles_root`,区分核心模型、来源和可选背景文件,避免把个人资料纳入 Skill 分发。
3. 内置 34 个人物核心及 94 篇来源蒸馏笔记,与外部用户资料分离;使用稳定身份和领域标签导航,普通应用只读核心,需要案例或证据时按需读取来源笔记.
4. 增加仅显式调用的推荐与思维风暴分支,按当前任务有界选模,比较不同视角与分歧;没有获准的并行能力时明确采用单 Agent 多视角推演.
5. 将来源整理限定为实际阅读材料后的独立归纳,保留出处与阅读范围;写入前核验路径和身份,先回读笔记再写核心,失败时核实实际状态并恢复本次修改.

## markdown-note-format (Markdown 笔记格式)

修改时间: 2026-09-08

1. 精炼 Skill 清单描述,保留创建,迁移,拆分和修复意图及格式覆盖,明确不读写真实笔记库,不判断知识价值或处理写入授权;职责与调用策略不变.

## new-skills (Skill 生命周期)

修改时间: 2026-09-08

1. 重构 Skill 生命周期入口,明确区分规划、只读审查、实施、更新检查和授权升级的边界。
2. 支持 Skill 实体目录与 Agent 发现目录分离,并补充安全的 Junction 或目录符号链接安装与本机设置规则。
3. 优化 Skill 集合发现与发布校验,新增合规顶层 Skill 可自动进入发布清单,无需维护重复路径列表。
4. 新增位于 `docs/` 的版本修改记录,二级标题同时标明 Skill 目录名和中文名称、独立记录修改时间,并让发布提交正文直接由当前版本记录生成。
5. 将快照导出、修改记录校验和提交说明生成合并为单一发布工具,统一复用配置与 Skill 成员发现规则。
6. 将公开贡献说明改为中文,并明确 Agent 获得提交授权后须先审查公私修改记录及当前项目文档、完成适用验证,再暂存和创建本地提交。
7. 规定普通更新递增补丁版本、正式发布递增次版本并将补丁位归零,提交说明直接由相应修改记录的最新一级章节生成。
8. 要求 description 变化时整体梳理表达,保留触发意图与必要排除,不以追加措辞或机械字数目标维护入口.
9. 将根文件与 docs 的公共导出改为路径黑名单,贡献指南与许可证从根目录唯一来源导出,README 使用仓库内相对链接;保留私有内容扫描,链接资源和误覆盖保护.

## orchestrate-subagents (纯总控多承载编排)

修改时间: 2026-09-08

1. 将编排 Skill 调整为仅显式调用的纯总控,明确内部子代理、独立任务和回调任务的承载边界。
2. 新增派发任务契约和按授权选择模型的分层规则,收紧文件所有权、递归委派、停止条件与验收责任。

## windows-codex-shell (Windows Codex Shell 诊断)

修改时间: 2026-09-08

1. 对结构化诊断证据执行严格类型校验,拒绝用字符串、数字或空值冒充布尔门禁证据。
2. 将未知或部分完成的写入统一判定为未解决结果,阻止重复执行、修复完成声明和后续执行交接。
3. 按 sandbox setup、工具启动和 Git ownership 分支分别定义完成证据,避免互相替代。

## windows-software-install (Windows 软件安装)

修改时间: 2026-09-08

1. 将下载、校验、安装、提权、环境修改、迁移、清理和重启的动作授权矩阵集中到实施阶段引用。
2. 明确只有进入授权实施模式才加载执行与恢复流程,规划或只读审计不触发安装器副作用。

# 0.0.0

## agently-mail (邮件操作,已移除)

修改时间: 2026-08-27

1. 新增基于 `agently-cli` 的 OAuth、邮件收发、回复、转发、搜索、附件处理和收件箱管理流程,并为写操作建立确认令牌与外部邮件内容不可信边界。
2. 将触发范围收窄到发送邮件和管理收件箱;建立公共 Skill 镜像时从本仓库移除该 Skill。

## capture-personal-knowledge (沉淀个人知识)

修改时间: 2026-08-19

1. 新增 `note`、`memory`、`no-save` 三路知识沉淀流程,分离价值判断、隐私脱敏、目标选择、写入意图和写后验证。
2. 为笔记写入增加 collection 范围校验、候选绑定、无覆盖创建和失败关闭事务,避免格式化能力直接获得真实笔记库的读写权限。
3. 允许用户当轮明确写入请求直接授权同批 `create`,将 `write_plan_digest` 收回为执行器内部完整性校验,非创建动作仍要求明确意图。

## markdown-note-format (Markdown 笔记格式)

修改时间: 2026-08-27

1. 新增 Markdown/Hexo 笔记的文件命名、目录拆分、YAML front matter、标签分类、摘要和本地图片路径规范。
2. 重构为只处理调用方已提供内容的不落盘候选生成器,将知识价值、collection 发现、动作授权和真实写入职责交还上游。
3. 用通用 YAML front matter profile 取代 Hexo 专用绑定,并完善候选协议、内容保留、资源映射以及公式和图表语法边界。
4. 明确 Mermaid、PlantUML、ditaa、legacy fence 和 draw.io 源文件/预览资源的选择与保留规则。

## new-skills (Skill 生命周期)

修改时间: 2026-08-27

1. 新增 Skill 创建、迁移、发现入口链接和官方格式验证流程,支持用户指定物理目录并保持单一内容真源。
2. 扩展为完整生命周期入口,加入本机设置、Junction 安全、内容风格、独立分发和通用静态校验工具。
3. 明确渐进式披露契约,要求触发信息留在 metadata、常驻规则留在正文、条件细节通过直接链接的一级引用加载。
4. 增加维护仓库到公共仓库的确定性快照发布,并提供带 24 小时 TTL、脏工作树保护、内容哈希、备份恢复和显式授权的更新检查与升级流程。

## orchestrate-subagents (纯总控多承载编排)

修改时间: 2026-08-19

1. 新增只负责拆解、路由、等待和验收的纯总控流程,区分当前任务内子代理与用户侧独立任务。
2. 补充混合承载、承载优先级、模型与推理等级映射、文件和状态所有权、失败恢复及结构化返回契约。

## windows-codex-shell (Windows Codex Shell 诊断)

修改时间: 2026-08-27

1. 新增 Windows `helper_unknown_error`、工具启动、sandbox 写入和特殊文件操作的安全诊断流程。
2. 收窄常规读取、搜索和 Git 查询的触发范围,并完善适用 `AGENTS.md`、最小命令和精确提权边界。
3. 建立 setup refresh、command runner、目标命令和 unknown outcome 的独立证据分类,增加 Git dubious ownership 路由、结构化证据评估器和连续验证门禁。

## windows-software-install (Windows 软件安装)

修改时间: 2026-08-12

1. 新增 Windows 大型第三方软件的来源核验、安装前检查、授权执行、安装位置选择、失败恢复和安装后验证流程。
