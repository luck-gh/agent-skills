---
name: bootstrap-project-context
description: 为新软件项目或沙盒收敛目标并建档,为首次接管的代码库沉淀上下文;也用于显式校准项目方向,按证据更新指定项目文档,收敛膨胀或陈旧的计划与能力记录.不用于普通开发,泛泛读码,排版润色或会话交接.
---

# 建立项目上下文基线

为软件项目建立最小,可信且可持续交接的事实基线,或按用户明确要求校准方向与项目文档.不继续实施功能,也不创建单次会话 handoff.

## 先区分请求

- 初始化或首次接管:只有建立或补齐持久文档的意图明确时进入基线流程.单纯讨论点子或只读了解代码库不触发初始化.
- 显式校准:用户明确要求判断项目偏航,依据目标与当前证据更新指定项目文档,或收敛其过时和重复条目时进入校准流程.不要求基线缺失,也不自动补建四份文档;只要求分析时不得写入.
- 两者都明确要求时先完成基线,再按已授权范围校准.不能从一次 Skill 调用推断用户批准了新的目标或纠偏实施.

## 定位项目根并检查指令入口

1. 按以下优先级确定项目根:用户明确指定的目标,当前工作目录所属的 Git root,唯一的 workspace root,当前目录.只有同时存在多个合理候选根时才询问用户.
2. 只用项目根直属的 `AGENTS.md` 与 `AGENTS.override.md` 判定根级契约,并按原始名称和原始路径类型检查.不得搜索父目录,子目录或全局位置来替代它.如果当前运行上下文已明确标出一个通过平台配置加载的根级非标准指令文件,停止并报告,不得创建会遮蔽它的 `AGENTS.md`;不要自行扫描全局配置猜测文件名.
3. 存在大小写不同的同名项,目录,符号链接,Junction,reparse point 或 broken path 时停止并报告,不得跟随或覆盖.
4. 根目录直属 `AGENTS.override.md` 会优先于待建或已有的 `AGENTS.md`,因此只要该原始路径存在就停止并报告.不得创建一个实际不会生效的根契约.

上述范围只用于判定根级契约.写入前仍须按平台优先级检查并遵守对实际目标文档生效的嵌套项目指令;例如普通的 `docs/AGENTS.override.md` 或 `docs/AGENTS.md`.合法的 override 优先级本身不是冲突.只有路径异常,无法唯一确定实际生效的指令,或有效指令与本次写入冲突时,才在任何写入前停止并报告.

完成入口检查后,完整读取已有普通 `AGENTS.md` 和[项目文档公共契约](references/project-document-contract.md).显式校准直接读取[显式校准与纠偏](references/explicit-calibration.md),跳过下方基线状态判定及两个初始化分支.公共契约的固定四文件预检与写入顺序仅用于建立或补齐基线,校准按实际授权目标预检和回读.

## 判定基线状态

仅对初始化或首次接管,按已读取的公共契约一次性检查 `AGENTS.md` 和三个目标文档,并分为唯一状态:

- 完整:基线步骤只读 no-op,不重写或重新初始化;仅要求初始化时退出,同时明确要求校准时继续校准分支.
- 可补齐:保留仍然成立的内容并继续.
- 冲突:在任何写入前停止并报告.

不得仅因普通 `AGENTS.md` 存在就判定完整.空文件不会构成有效指令;非空文件也可能只覆盖部分项目规则.可补齐时只追加缺失且兼容的根级职责.

## 按实现证据选择分支

只有状态为可补齐时,再根据项目证据恰好读取一个分支说明:

- 没有有意义的源码,构建配置,依赖清单或测试证据时,读取[空白项目基线](references/blank-project.md).
- 已有任一有意义的源码,构建配置,依赖清单或测试证据时,读取[现有代码库基线](references/existing-project.md).

版本控制元数据,编辑器配置,许可证或孤立的说明文档本身不构成已有实现证据.不要读取未选中的分支说明.

## 共同边界与完成标准

- 状态为可补齐时,按已读取的公共契约完成信息路由,写入顺序和一致性验收.
- 不写项目源码或配置,不安装或升级依赖,不改变 Git index.禁止运行 `git add`,`git commit`,`git push`,`git merge`,`git reset` 或 `git checkout`.
- `AGENTS.md` 保存稳定纠偏规则,`PLAN.md` 保存当前纠偏或其唯一真源指针.疑似偏航不等于获准改目标;纠偏文件没有天然最高优先级,不得创建 `AGENTS.override.md` 来强制生效.
- `PLAN.md` 与 `FEATURES.md` 分别维护项目方向与当前主要能力,不是任务或测试日志.新增和已有条目都先按公共契约判断入档资格,再决定更新或移除;曾经写入,状态变化或仍需行动不构成单独的保留理由.局部工作可不更新两文档;纠正失实描述不等于持续追踪局部事项,已关闭条目不改写为完成日志.保留符合职责的重要风险,有效决策与必要证据,不按日期或篇幅机械裁剪.
- 开放性分析允许自行选择合适方法与只读工具;写入必须完成路径,授权,证据及敏感信息预检,保留有效内容并回读.失败或部分写入时停止并报告实际状态,不宣称完成.
- 按所选分支完成一致性验收.只读校准交付证据与判断;获准写入后交付已回读的文档及未解决项.不把文档计划当作实施或验证结果.
- 完成后结束当前工作并等待用户确认,不得继续编码.受纠偏影响的工作恢复前显式重读相关规则与活动条目,不假定运行中的任务会自动刷新上下文.

交付时简洁列出项目根,采用的模式,基线状态或校准结论,实际读写范围,纠正项,保留项,验证结果及待确认事项.
